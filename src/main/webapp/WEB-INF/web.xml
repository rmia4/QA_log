<?xml version="1.0" encoding="UTF-8"?>
<!-- Servlet 3.0(Tomcat 7 실제 구현 버전)으로 선언 - 3.1로 선언하면 mvn tomcat7:run 실행 시
     ClassCastException이 난다(pom.xml의 javax.servlet-api 버전 조정 사유 참고) -->
<web-app xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee
                              http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
         version="3.0">

    <display-name>QA로그</display-name>

    <session-config>
        <session-timeout>43200</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
            <max-age>2592000</max-age>
        </cookie-config>
    </session-config>

    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/spring/root-context.xml</param-value>
    </context-param>

    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    <servlet>
        <servlet-name>appServlet</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring/appServlet/servlet-context.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
        <!-- 첨부파일 업로드(오류 등록 시 스크린샷) - Servlet 3.1 표준 멀티파트, 별도 라이브러리 불필요 -->
        <multipart-config>
            <max-file-size>10485760</max-file-size>      <!-- 10MB -->
            <max-request-size>52428800</max-request-size> <!-- 50MB -->
        </multipart-config>
    </servlet>

    <servlet-mapping>
        <servlet-name>appServlet</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

    <!-- 리버스 프록시(GitHub Codespaces 포트 전달 등) 뒤에서 돌 때, X-Forwarded-Host/-Proto/-Port
         헤더를 읽어 request.getServerName()/getServerPort()/getScheme()을 외부에서 보이는 값으로
         맞춰준다 - 이게 없으면 response.sendRedirect("/login") 같은 상대경로 리다이렉트를 절대경로로
         바꿀 때 Tomcat이 자기가 실제로 붙어있는 내부 포트(예: 8080)를 그대로 써버려서, 로그인 등
         거의 모든 화면 전환이 "우리계정.app.github.dev:8080" 같은 존재하지 않는 주소로 튕기는
         문제가 생긴다(2026-09-14 Codespaces로 공유하다가 재현해서 발견). 반드시 다른 필터보다
         먼저 실행돼야 하므로 맨 앞에 둔다. -->
    <filter>
        <filter-name>forwardedHeaderFilter</filter-name>
        <filter-class>org.springframework.web.filter.ForwardedHeaderFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>forwardedHeaderFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>forceEncoding</param-name>
            <param-value>true</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Tomcat 기본 에러 페이지(스택 트레이스·내부 경로 노출)를 그대로 보여주지 않고 /error로 모아서
         처리한다(egovframework.common.controller.ErrorPageController, 2026-09-14 팀 결정) -->
    <error-page>
        <error-code>400</error-code>
        <location>/error</location>
    </error-page>
    <error-page>
        <error-code>403</error-code>
        <location>/error</location>
    </error-page>
    <error-page>
        <error-code>404</error-code>
        <location>/error</location>
    </error-page>
    <error-page>
        <error-code>409</error-code>
        <location>/error</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/error</location>
    </error-page>
    <error-page>
        <exception-type>java.lang.Exception</exception-type>
        <location>/error</location>
    </error-page>

</web-app>
