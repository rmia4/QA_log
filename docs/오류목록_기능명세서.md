# 오류 목록 기능 명세서 v0.1

## 표시 항목

오류 목록에는 번호, 오류 제목, 상태, 심각도, 우선순위, 담당자, 등록일을 표시한다.

우선순위 표시값은 다음과 같다.

| 저장값 | 화면 표시 |
|---|---|
| `unspecified` | 미지정 |
| `low` | 낮음 |
| `normal` | 보통 |
| `high` | 높음 |
| `urgent` | 긴급 |

## 정렬

- 정렬 버튼은 오류 목록 우측 상단에 심각도, 우선순위, 추가 날짜 순서로 표시한다.
- 기본 정렬은 심각도 내림차순이다.
- 현재 선택된 정렬 버튼을 다시 누르면 오름차순과 내림차순을 전환한다.
- 우선순위 내림차순은 긴급, 높음, 보통, 낮음, 미지정 순서다.
- 우선순위 오름차순은 이 순서의 반대다.
- 같은 정렬값을 가진 오류는 최신 등록 항목부터 표시한다.
